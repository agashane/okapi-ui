---
import type { ComponentProps } from "astro/types";
import { Text } from "../typography";

type Props = ComponentProps<typeof Text>;

const { ...props } = Astro.props;
---

<Text {...props} data-slot="alert-dialog-description"><slot /></Text>
